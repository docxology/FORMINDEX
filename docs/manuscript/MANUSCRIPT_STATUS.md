# Manuscript status — FORMINDEX

## Repo type
Data-analysis / visualization project (evidence: `Methods/`, `Scripts/`,
`LLM_Methods/`, generated `Reports/`, `Visualizations/`, `Targeted_Bibliographies/`;
no `manuscript/` directory and no packaging metadata in the repo).

## Why no publication-target manuscript applies today
The repo produces reports and visualizations from the FORMIS database export
(`Reports/FORMINDEX_10_13_2024.pdf`, earlier dated PDFs) rather than a
section-structured research manuscript managed under the template standard
(`00_abstract.md` … `99_references.md`, `config.yaml`). There is no
`manuscript/` or `docs/manuscript/` content to audit.

## What would trigger creating one
Promoting the existing dated report PDFs into a section-structured,
config-governed manuscript (with claim-provenance variables) if FORMINDEX moves
onto the template publication pipeline.
